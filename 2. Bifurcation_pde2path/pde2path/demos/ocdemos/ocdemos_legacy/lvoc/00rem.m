% p2p-dir for LV OC (boundary), not quite cleaned up!
% bd1.m: script for CSS
%  cp.m: script for CPs
%
% cfi*: G: rhs [c,a,f,b] 
%      bc: boundary conditions 
%     con: compute control from (v,lam) 
%     jcf: objective 
%
% rest: mods of default p2p functions for adapted plotting etc ...
%       and, e.g., iscnat, mod of p2poc-func, which resets the initial
%       states to v=1 (steady-state ohne harvesting)
